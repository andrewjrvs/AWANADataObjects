export class CoreBook {

    /** the books internal ID, maybe ISBN number or something */
    identifier: string;
    /** this is the name of the book, like wing runner */
    name: string;

    createdDate: string;
    
    // Need to show sections somehow
}